<Page
    x:Class="FanKit.Transformer.TestApp.CarouselsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:FanKit.Transformer.TestApp"
    xmlns:xaml="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    >

    <Grid>

        <xaml:CanvasAnimatedControl x:Name="CanvasControl" Paused="True"/>

        <Border Margin="12" Width="253" HorizontalAlignment="Left" VerticalAlignment="Top" Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="32"/>

                    <RowDefinition Height="8"/>
                    <RowDefinition Height="26"/>
                    <RowDefinition Height="32"/>

                    <RowDefinition Height="8"/>
                    <RowDefinition Height="26"/>
                    <RowDefinition Height="32"/>

                    <RowDefinition Height="8"/>
                    <RowDefinition Height="26"/>
                    <RowDefinition Height="32"/>

                    <RowDefinition Height="8"/>
                    <RowDefinition Height="26"/>
                    <RowDefinition Height="32"/>

                    <RowDefinition Height="12"/>
                </Grid.RowDefinitions>

                <Border Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
                    <TextBlock Margin="14,0" VerticalAlignment="Center" Text="Parameter"/>
                </Border>

                <Grid Grid.Row="2" Margin="14,0,8,0">
                    <TextBlock HorizontalAlignment="Left" Text="Skew"/>
                    <TextBlock x:Name="SkewTextBlock" HorizontalAlignment="Right" FontWeight="Bold">
                        0%
                    </TextBlock>
                </Grid>
                <Slider x:Name="SkewSlider" Margin="12,0" Grid.Row="3" IsThumbToolTipEnabled="False"/>

                <Grid Grid.Row="5" Margin="14,0,8,0">
                    <TextBlock HorizontalAlignment="Left" Text="Margin"/>
                    <TextBlock x:Name="MarginTextBlock" HorizontalAlignment="Right" FontWeight="Bold">
                        0
                    </TextBlock>
                </Grid>
                <Slider x:Name="MarginSlider" Margin="12,0" Grid.Row="6" IsThumbToolTipEnabled="False"/>

                <Grid Grid.Row="8" Margin="14,0,8,0">
                    <TextBlock HorizontalAlignment="Left" Text="Spacing"/>
                    <TextBlock x:Name="SpacingTextBlock" HorizontalAlignment="Right" FontWeight="Bold">
                        0
                    </TextBlock>
                </Grid>
                <Slider x:Name="SpacingSlider" Margin="12,0" Grid.Row="9" IsThumbToolTipEnabled="False"/>

                <TextBlock Grid.Row="11" Margin="14,0,8,0" HorizontalAlignment="Left" Text="GRid LInes"/>
                <ToggleSwitch x:Name="ShowGridButton" Grid.Row="12" Margin="12,0,0,0"/>

            </Grid>
        </Border>

    </Grid>
</Page>