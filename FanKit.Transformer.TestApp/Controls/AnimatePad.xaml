<Canvas
    x:Class="FanKit.Transformer.TestApp.AnimatePad"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:FanKit.Transformer.TestApp"
    mc:Ignorable="d"
    d:DesignWidth="400"
    d:DesignHeight="400"
    >

    <Canvas x:Name="Canvas" Width="200" Height="236" Background="Transparent">
        <Thumb x:Name="Thumb" Width="200" Height="200" >
            <Thumb.Template>
                <ControlTemplate>
                    <Rectangle Width="200" Height="200">
                        <Shape.Fill>
                            <SolidColorBrush Opacity="0.2" Color="LightSeaGreen"/>
                        </Shape.Fill>
                    </Rectangle>
                </ControlTemplate>
            </Thumb.Template>
        </Thumb>

        <Rectangle IsHitTestVisible="False" Width="200" Height="200" StrokeThickness="4" Stroke="LightSeaGreen"/>
        <Rectangle IsHitTestVisible="False" Canvas.Top="200" Width="200" Height="36" Fill="LightSeaGreen"/>

        <Rectangle IsHitTestVisible="False" Canvas.Left="4" Canvas.Top="200" Width="192" Height="32" Fill="{ThemeResource SystemControlPageBackgroundChromeLowBrush}"/>
        <Slider x:Name="Slider" Canvas.Left="10" Canvas.Top="200" Width="180" Height="32"/>
    </Canvas>
</Canvas>