<UserControl
    x:Class="FanKit.Transformer.TestApp.BottomBar2"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:FanKit.Transformer.TestApp"
    mc:Ignorable="d"
    d:DesignWidth="400"
    d:DesignHeight="400"
    Margin="8"
    HorizontalAlignment="Center"
    VerticalAlignment="Bottom"
    >

    <ScrollViewer
        VerticalScrollMode="Disabled"
        HorizontalScrollMode="Enabled"
        VerticalScrollBarVisibility="Disabled"
        HorizontalScrollBarVisibility="Hidden"
        Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}"
        >
        <StackPanel Margin="14,8" Orientation="Horizontal">

            <TextBlock Margin="8,0" VerticalAlignment="Center" Text="HIde Dock Panel"/>
            <ToggleSwitch x:Name="HideButton" Width="86"/>

            <TextBlock Margin="8,0" VerticalAlignment="Center" Text="Touch Simulator"/>
            <ToggleSwitch x:Name="TouchButton" Width="86"/>

            <TextBlock Margin="8,0" VerticalAlignment="Center" Text="Settings"/>
            <ToggleButton x:Name="EventsButton" Margin="0,2" Content="Events"/>

        </StackPanel>
    </ScrollViewer>
</UserControl>