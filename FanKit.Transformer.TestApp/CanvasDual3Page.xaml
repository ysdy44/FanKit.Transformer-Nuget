<Page
    x:Class="FanKit.Transformer.TestApp.CanvasDual3Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:FanKit.Transformer.TestApp"
    xmlns:xaml="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    >

    <Grid>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <xaml:CanvasControl x:Name="CanvasControlDestination" Grid.Column="2"/>
            <xaml:CanvasControl x:Name="CanvasControlSource"/>
            <local:AnimatePad x:Name="AnimatePad" Visibility="Collapsed"/>
            <local:TouchPad x:Name="TouchPad" Visibility="Collapsed"/>

            <Rectangle Grid.Column="1" Fill="{ThemeResource SystemControlBackgroundChromeMediumBrush}"/>
        </Grid>

        <local:ToolBox3 x:FieldModifier="public" x:Name="ToolBox3" Title="Free Transform"/>

        <Border x:Name="ThumbBorder" Margin="12" Width="253" HorizontalAlignment="Right" VerticalAlignment="Top" Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="32"/>
                    <RowDefinition Height="166"/>
                </Grid.RowDefinitions>
                <Border Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
                    <TextBlock Margin="14,0" VerticalAlignment="Center" Text="Thumbnail"/>
                </Border>
                <xaml:CanvasControl x:FieldModifier="public" x:Name="ThumbCanvasControl" Grid.Row="1"/>
            </Grid>
        </Border>

        <local:FreeTransformPanel x:FieldModifier="public" x:Name="ParameterPanel"/>

        <local:BottomBar3 x:Name="BottomBar"/>

        <local:CanvasHub3 x:Name="CanvasHub"/>

        <local:SettingsDialog3 x:Name="SettingsDialog" Visibility="Collapsed"/>

    </Grid>
</Page>